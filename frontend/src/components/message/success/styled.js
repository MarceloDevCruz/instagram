import styled from 'styled-components';
import { sucess, danger, info, alert } from '../../../styles/Colors'

export const MessageStyled = styled.div`

    span {
      font-size: 1.2em;
      color: ${sucess};
    }

` 